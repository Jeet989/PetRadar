export interface epState {
    Description: string, 
    time:String,
    
}

export interface epthReducer {
    Description: string, 
    time:String,
    
}

export interface epReducerData {
   postId:string,
   location:string,
    Description: string,
    time:String,
}

